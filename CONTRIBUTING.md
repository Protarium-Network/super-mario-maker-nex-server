# Contributing

Format changed Go files with `gofmt`, then run `go vet ./...` and `go test ./...`. Keep pull requests focused and describe any behavior verified on real hardware without attaching private console material.

Do not commit credentials, console certificates, copyrighted game assets, database dumps, uploaded courses without permission, or unredacted packet captures.
nk
